<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Główny motyw aplikacji (dopasuj nazwę do swojej aplikacji jeśli trzeba) -->
    <style name="Theme.Projekt_Czezowska_Dominika_kosmetyki" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Tutaj mogą być Twoje kolory aplikacji -->
    </style>

    <!-- Styl zaokrąglenia dla ShapeableImageView -->
    <style name="RoundedSquare" parent="ShapeAppearance.MaterialComponents.MediumComponent">
        <item name="cornerSizeTopLeft">14dp</item>
        <item name="cornerSizeTopRight">14dp</item>
        <item name="cornerSizeBottomLeft">0dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
    </style>
</resources>